# Emulator-only values. Copy to .secret.local; never use production keys in tests.
OPENAI_API_KEY=emulator-not-a-real-key
REVENUECAT_SECRET_KEY=emulator-not-a-real-key
REVENUECAT_WEBHOOK_SECRET=emulator-webhook-secret
