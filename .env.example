# Public client configuration is in dart_defines.example.json.
# Node/PostgreSQL runtime and Docker settings: backend/.env.example.
# Server secrets use protected files on the VPS, never Flutter or public hosting.
FIREBASE_PROJECT_ID=leanguard-a58ff
BACKEND_BASE_URL=https://api.example.com
# Firebase is used only for Authentication and FCM.
# Follow docs/BACKEND_VPS.md; no Cloud Functions deployment is required.
